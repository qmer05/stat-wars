{
  "name": "@stat-wars/server",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npx wrangler dev",
    "deploy": "npx wrangler deploy",
    "check": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250813.0",
    "typescript": "^5.5.4",
    "wrangler": "^4.30.0"
  },
  "dependencies": {
    "@stat-wars/shared": "file:../../packages/shared"
  }
}
